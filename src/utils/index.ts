import { request } from "./request";
import {
    setLocalToken,
    getLocalToken,
    clearLocalToken
} from './token'


export {
    request,
    setLocalToken,
    getLocalToken,
    clearLocalToken
}